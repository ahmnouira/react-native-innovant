import {ThemeType} from './theme'
import {ThemeContext, ThemeContextProps, ThemeProvideProps, ThemeProvider} from './theme.context'


export type {
    ThemeType,
    ThemeContextProps, 
    ThemeProvideProps

}

export {
    ThemeContext,
    ThemeProvider
}

