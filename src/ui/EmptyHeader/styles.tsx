import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    marginRight: 12,
    height: 40,
    width: 40,
    borderRadius: 50,
    backfaceVisibility: 'hidden',
  },
});
