import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  main: {
    marginHorizontal: 8,
    flexDirection: 'row',
  },

  genresView: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
})
